pretty sure the button will not be there, if the user, uses another language than english : todo find the parent of the list in injectBelowWhatsNew()

find a less loopy way of attaching the button injectBelowWhatsNew(), instead of using setInterval

make a small gif how it works